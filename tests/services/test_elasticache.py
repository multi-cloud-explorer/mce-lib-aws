#from moto import mock_el

from mce_lib_aws.services import elasticache as service

import pytest

@pytest.mark.mce_todo
def test_elastic_cache(aws_session, aws_region, aws_account_id):
    raise  NotImplementedError()
